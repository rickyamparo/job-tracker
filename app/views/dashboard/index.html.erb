This is the Dashboard for your JobTracker

Job Count by level of interest:

<% @jobs.each do |job| %>
  <ul><%= job.title %> at <%= job.company.name %><br />
    <%= link_to "Edit", edit_company_job_path(job.company, job) %>
    <%= link_to "Delete", company_job_path(job.company, job), method: "delete" %>
  </ul>
<% end %>
